{
  "pluginAlias": "Meross",
  "pluginType": "accessory",
  "singular": true,
  "headerDisplay": "<p align='center'><img width='250px' src='https://raw.githubusercontent.com/donavanbecker/homebridge-meross/master/meross/02.png'></p>\n\nThe **Homebridge Meross** plugin allows you to control your Meross Devices from HomeKit. To get started setup your config below.",
  "footerDisplay": "Your Meross device is setup. Please raise any issues on our [project page](https://github.com/donavanbecker/homebridge-honeywell-home/issues).",
  "schema": {
    "type": "object",
    "properties": {
      "name": {
        "title": "Name",
        "type": "string",
        "default": "Meross",
        "required": true
      },
       "deviceUrl": {
        "title": "Device IP Address",
        "type": "string",
        "required": true,
        "format": "hostname",
        "required": true
      },
        "hardwareVersion": {
        "title": "Hardware Version",
        "type": "integer",
        "required": true,
        "format": "hostname",
        "required": true
      },
        "channel": {
        "title": "Device Channel",
        "type": "integer",
        "required": true,
        "format": "hostname",
        "required": true
      },
        "messageId": {
        "title": "MessageID",
        "type": "string",
        "required": true,
        "format": "hostname",
        "required": true
      },
        "timestamp": {
        "title": "TimeStamp",
        "type": "integer",
        "required": true,
        "format": "hostname",
        "required": true
      },
        "sign": {
        "title": "Sign",
        "type": "string",
        "required": true,
        "format": "hostname",
        "required": true
      }
      }
    }
  },
    "layout": [
      {
        "type": "fieldset",
        "title": "Advanced Settings",
        "expandable": true,
        "expanded": false,
        "items": [
          {
            "type": "help",
            "helpvalue": "<h5>TTL</h5><em class='primary-text'>TTL indicates the number of seconds between polls of the Honeywell Home service.</em>"
          },
          {
            "key": "options.ttl",
            "notitle": true
          },
          {
            "type": "help",
            "helpvalue": "<h5>Debug Mode</h5><em class='primary-text'>Indicate if debug level logging should be enabled.</em>"
          },
          "options.debug"
        ]
      }
    ]
}
